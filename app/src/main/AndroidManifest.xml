<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="id.java.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme2">

        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".Home"></activity>

        <activity android:name=".Panduan"></activity>

        <activity android:name=".DetailActivity"></activity>

        <activity android:name=".Tentang"></activity>

        <activity android:name=".Paket"></activity>

        <activity android:name=".Promo"></activity>

        <activity android:name=".Persiapan"></activity>

        <activity android:name=".Larangan"></activity>

        <activity android:name=".PHaji"></activity>

        <activity android:name=".PUmroh"></activity>

    </application>

</manifest>